import "./Marquee.css";

/*props: {
  contents: string[],
  speed: number, px per second
}*/

export const Marquee = (props) => {
  const { contents, speed } = props;

  return (
    <div className="marquee">
      
    </div>
  );
};
